# Agenda Backoffice — Cultura Sabauda
# Installer avec : crontab crontab.txt   (puis vérifier : crontab -l)
#
# Chemins = déploiement VPS réel : projet sous /root/evenements, interpréteur du
# venv (.venv/bin/python). Adapter si le projet est ailleurs.
# Heures en heure locale du serveur (timezone Europe/Paris — voir timedatectl).

# Scraping RSS quotidien 8h
0 8 * * * cd /root/evenements && .venv/bin/python scripts/scraper_events.py >> logs/scraper.log 2>&1
# Collecte des newsletters (label Gmail « Agenda ») 8h15
15 8 * * * cd /root/evenements && .venv/bin/python scripts/gmail_collect.py >> logs/gmail.log 2>&1
# Pré-datation GRATUITE 8h25 — parsing texte seul (ni téléchargement de page, ni LLM :
# aucun coût, aucun quota consommé). Sert UNIQUEMENT à armer la garde de dates de
# dedupe (_dates_incompatible) sur les fiches scrapées le matin même : sans elle, la
# garde ne protège que ce qui a déjà été daté un jour précédent, or c'est justement le
# flux du jour qui fabrique les fusions à tort (rubriques récurrentes « COSA FARE… »,
# « Que faire à Nice ce week-end du… », voir l'audit du 2026-08-02). --no-republish :
# la passe 4 (traductions) reste au run complet de 8h45, on ne la double pas.
25 8 * * * cd /root/evenements && .venv/bin/python scripts/dates.py --no-fetch --no-llm --no-republish >> logs/dates.log 2>&1
# Déduplication multi-sources 8h30 (avant l'évaluation → évite d'évaluer des doublons)
30 8 * * * cd /root/evenements && .venv/bin/python scripts/dedupe.py >> logs/dedupe.log 2>&1
# Datation 8h45 : extrait la vraie date d'événement (déterministe, gratuit) → filtres par période
45 8 * * * cd /root/evenements && .venv/bin/python scripts/dates.py >> logs/dates.log 2>&1
# Évaluation LLM 9h (après scraping)
# NB : le cron évalue TOUTE la file pending (rattrapage quotidien). Le pilotage par
# période (« ce week-end ») se fait à la MAIN via le bouton du dashboard.
0 9 * * * cd /root/evenements && .venv/bin/python scripts/evaluator.py >> logs/evaluator.log 2>&1
# Lot quotidien 9h30 (après évaluation) — REMPLACE les crons séparés enrich/publish du
# 2026-07-31 (voir docs/BACKLOG.md, "Protocole de LOT", 2026-08-01) : scripts/daily_batch.py
# enchaîne rédaction → vérification (mêmes règles que batch_report.py : score, article,
# panel, date exploitable) → publication (image réelle incluse) → notification Slack, et
# NE PUBLIE QUE LES ÉVÉNEMENTS COMPLETS. Les incomplets restent en base pour le lendemain,
# listés dans le message Slack avec la raison précise (rien de silencieux). DAILY_BATCH_SIZE
# (.env, défaut 10) règle la taille du lot — --cap l'outrepasse ponctuellement.
30 9 * * * cd /root/evenements && .venv/bin/python scripts/daily_batch.py >> logs/daily_batch.log 2>&1
# SEO quotidien 10h30 (après le lot du jour, sur le haut du panier score ≥ 7) — republie
# automatiquement ce qui était déjà en ligne (sinon le SEO reste invisible en base). Petit
# cap volontaire (coût LLM par événement) — ajuste --cap dans le script si besoin.
30 10 * * * cd /root/evenements && .venv/bin/python scripts/seo_batch.py --cap 10 >> logs/seo_batch.log 2>&1
# Traduction FR/IT quotidienne 10h45 — ⏸️ EN PAUSE depuis le 2026-08-01 (soir).
# Motif : une fiche traduite publiée en ligne (WP#6798) porte le TITRE d'un événement et
# le lieu/les dates d'un autre. La cause n'est pas élucidée ; l'hypothèse en cours est une
# contamination ANTÉRIEURE à la traduction (fusion de doublons ou scraping), le traducteur
# ne faisant que révéler une matière déjà polluée. Tant que ce n'est pas tranché, ce cron
# fabriquerait chaque jour des fiches potentiellement fausses, publiées en ligne.
# ⚠️ Les correctifs de dates (dates.py excluant les traductions) et de cycles sont, eux,
# déjà en place — cette pause ne concerne QUE la création de nouvelles traductions.
# RÉACTIVER en décommentant, une fois la contamination expliquée (cf. docs/BACKLOG.md).
#45 10 * * * cd /root/evenements && .venv/bin/python scripts/translate_events.py --apply --cap 5 >> logs/translate.log 2>&1
# Santé de la home 13h — alerte Slack si « À la une »/« En évidence » se vident (cf.
# incident du 2026-07-31, découvert par hasard faute de surveillance). Zéro coût, se
# resserre facilement si besoin de plus de fréquence.
0 13 * * * cd /root/evenements && .venv/bin/python scripts/homepage_health.py >> logs/homepage_health.log 2>&1
# Nettoyage hebdomadaire dimanche 5h — chaîne RÉVERSIBLE et DÉTERMINISTE uniquement
# (purge hors-zone/passés/incomplétables, articles de presse publiés à tort, doublons
# WordPress, sources non institutionnelles, audit visuel borné) — voir scripts/weekly_audits.py
# pour le détail et pourquoi CES scripts-là précisément sont sans risque à automatiser.
0 5 * * 0 cd /root/evenements && .venv/bin/python scripts/weekly_audits.py >> logs/weekly_audits.log 2>&1
# Digest hebdomadaire lundi 8h — état des automatisations + reste à faire, posté sur
# Slack (équivalent de scripts/status_report.py, à lancer à la main pour un état à jour
# à tout moment, y compris pour coller le résultat à une IA qui reprend le projet).
0 8 * * 1 cd /root/evenements && .venv/bin/python scripts/weekly_digest.py >> logs/weekly_digest.log 2>&1
# Sauvegarde de la base 3h (copie cohérente horodatée, garde les 14 dernières) — anti perte de données
0 3 * * * cd /root/evenements && .venv/bin/python scripts/backup_db.py >> logs/backup.log 2>&1
