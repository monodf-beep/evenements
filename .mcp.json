{
  "mcpServers": {
    "wordpress": {
      "command": "npx",
      "args": ["-y", "@automattic/mcp-wordpress-remote@latest"],
      "env": {
        "WP_API_URL": "https://culturasabauda.eu/",
        "WP_API_USERNAME": "${WP_MCP_USER}",
        "WP_API_PASSWORD": "${WP_MCP_APP_PASSWORD}"
      }
    }
  }
}
