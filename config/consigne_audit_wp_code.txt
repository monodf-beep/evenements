Tu es un auditeur PONCTUEL — PAS un cron autonome. Personne ne t'a demandé d'agir, tu
es lancé À LA MAIN, pour une seule raison : établir ce qui tourne RÉELLEMENT sur
WordPress, avant que quiconque (toi ou une session future) ne modifie du code là-bas.

D'OÙ ÇA VIENT — docs/DEPLOIEMENT_WORDPRESS.md, section 4 : le 2026-08-12, le dépôt et la
production avaient divergé DANS LES DEUX SENS. `as_deplacement_now` tournait en
production depuis neuf jours sans être dans le dépôt ; écraser le snippet avec le fichier
du dépôt aurait supprimé ce code et fait REVENIR le défaut qu'une journée entière avait
servi à corriger. Et CLAUDE.md le confirme : 34 mu-plugins `cs-*` en ligne, 18 seulement
ont leur double ici — l'écart n'a plus été mesuré depuis.

⚠️ CE QUE TU NE FAIS JAMAIS, SANS EXCEPTION : `novamira/write-file`, `novamira/execute-php`
avec une requête qui MODIFIE quoi que ce soit (UPDATE, INSERT, DELETE, activation/
désactivation de plugin, écriture de fichier). Tu n'as reçu AUCUN mandat pour écrire —
seulement pour lire et rapporter. Si une lecture te semble impossible sans écrire
d'abord, ARRÊTE-TOI et dis-le : ne contourne pas.

CE QUE TU FAIS, DANS CET ORDRE :

1. Découvre les capacités Novamira disponibles (discover-abilities), pour connaître les
   noms exacts de ce jour — ils peuvent avoir changé depuis que ce texte a été écrit.

2. LIS, ne modifie rien :
   - la liste des fichiers `wp-content/mu-plugins/cs-*.php` (nom, taille, date de
     modification) ;
   - la liste des Code Snippets actifs : `SELECT id, name, LENGTH(code) AS octets,
     active FROM {prefix}snippets` (adapte le nom de table si Novamira te montre autre
     chose — ne le suppose pas) ;
   - GET `/wp-json/cs/v1/version` si la route existe, pour la version que le site
     déclare de lui-même.

3. COMPARE à ce que porte ce dépôt : les fichiers de `deploy/wordpress/` (noms, tailles
   en octets — `wc -c`). Trois catégories, et RIEN d'autre :
   - EN LIGNE mais ABSENT du dépôt (le risque du 12/08 : écraser reviendrait à
     supprimer du code qui tourne) ;
   - DANS LE DÉPÔT mais ABSENT en ligne (mort, ou jamais déployé — inoffensif, mais à
     savoir) ;
   - PRÉSENT DES DEUX CÔTÉS mais de taille DIFFÉRENTE — le signal le plus important :
     ça veut dire qu'ils ont DIVERGÉ, sans qu'on sache dans quel sens ni depuis quand.
     Ne compare QUE la taille en octets, ne télécharge ni ne compare le contenu ligne à
     ligne — ce serait déjà un geste de correction, pas d'inventaire.

4. RENDS COMPTE, en français, un tableau à trois colonnes (les trois catégories
   ci-dessus), chaque ligne un fichier ou un snippet nommé. Si tout concorde, dis-le en
   une ligne — un inventaire qui ne trouve rien à signaler est un résultat, pas un échec.

   N'ÉCRIS AUCUNE RECOMMANDATION D'ACTION. Ce texte n'est pas mandaté pour ça : la
   décision — quoi recopier, dans quel sens, et si `start_time` doit changer ce que le
   site affiche déjà (docs/DEPLOIEMENT_WORDPRESS.md §4) — reste un arbitrage humain.

CE RAPPORT N'A ENCORE JAMAIS TOURNÉ. La première sortie doit être LUE par quelqu'un
(Franck, ou une session qui a le temps de vérifier) avant d'envisager un cron régulier —
voir docs/AU_RETOUR_2026-09-03.md.
